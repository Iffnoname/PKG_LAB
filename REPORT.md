# Сопроводительная документация

## Лабораторная работа 1 — Цветовые модели

**Вариант №4:** RGB ↔ HSV ↔ LAB

---

## 1. Цель работы

Изучить цветовые модели RGB, HSV и CIE L\*a\*b\*, алгоритмы перехода между ними и реализовать интерактивное приложение, отображающее один и тот же цвет в трёх моделях одновременно.

## 2. Постановка задачи

По методичке необходимо:

- показывать составляющие цвета в **трёх** моделях;
- задавать цвет **точным вводом**, **палитрой** и **плавным изменением** (ползунки);
- **автоматически** пересчитывать две другие модели при изменении любой координаты;
- при «некорректных» результатах (выход за gamut RGB) выводить **ненавязчивое** предупреждение.

## 3. Вариант №4

Требуемые преобразования: **RGB ↔ HSV ↔ LAB**.

Прямого преобразования HSV ↔ LAB в коде нет: используется **RGB как опорная модель** (HSV → RGB → LAB и LAB → RGB → HSV), что математически эквивалентно корректной цепочке.

## 4. Теоретические сведения

### RGB

Аддитивная модель для дисплеев. Каналы R, G, B ∈ [0, 255]. В расчётах к XYZ используется **sRGB** с кусочно-линейной гамма-функцией.

### HSV

Hue (тон), Saturation (насыщенность), Value (яркость). В приложении: H ∈ [0, 360), S, V ∈ [0, 100] %.

### CIE XYZ и LAB

**XYZ** — линейное пространство, связанное с восприятием. **L\*a\*b\*** — perceptually uniform модель относительно белого **D65**.

Промежуточный шаг для Lab: **RGB → XYZ → LAB**.

## 5. Математические формулы (как в `color_converter.py`)

### RGB → HSV (каналы 0–255)

Нормализация \(r, g, b \in [0,1]\). \(C_{\max} = \max(r,g,b)\), \(C_{\min} = \min(r,g,b)\), \(\Delta = C_{\max} - C_{\min}\).

- Если \(\Delta = 0\): \(H = 0\).
- Иначе \(H = 60^\circ \cdot f(r,g,b)\) по стандартному секторному правилу (см. код).
- \(S = 0\) при \(C_{\max}=0\), иначе \(S = \Delta / C_{\max} \cdot 100\%\).
- \(V = C_{\max} \cdot 100\%\).

### HSV → RGB

\(S' = S/100\), \(V' = V/100\), \(C = V' S'\), \(X = C(1 - |(H/60) \mod 2 - 1|)\), \(m = V' - C\), затем секторы 0°–360° и умножение на 255 с округлением.

### sRGB ↔ линейный RGB

\[
C_{\text{lin}} = \begin{cases}
C / 12.92, & C \le 0.04045 \\
\left(\frac{C + 0.055}{1.055}\right)^{2.4}, & \text{иначе}
\end{cases}
\]

Обратная **gamma** для XYZ → RGB — симметричная формула в коде (`_gamma_srgb`).

### RGB → XYZ (матрица sRGB D65, Y=100)

После линейзации:

\[
\begin{bmatrix} X \\ Y \\ Z \end{bmatrix} = 100 \cdot
\begin{bmatrix}
0.4124564 & 0.3575761 & 0.1804375 \\
0.2126729 & 0.7151522 & 0.0721750 \\
0.0193339 & 0.1191920 & 0.9503041
\end{bmatrix}
\begin{bmatrix} R \\ G \\ B \end{bmatrix}_{\text{lin}}
\]

### XYZ → RGB

Обратная матрица (коэффициенты в `xyz_to_rgb`), затем gamma, clipping к [0, 255].

### XYZ ↔ LAB (D65: \(X_n=95.047, Y_n=100, Z_n=108.883\))

\(f(t)\): при \(t > \varepsilon\) — \(t^{1/3}\), иначе \((\kappa t + 16)/116\), \(\varepsilon = 216/24389\), \(\kappa = 24389/27\).

\[
L^* = 116 f(Y/Y_n) - 16,\quad
a^* = 500(f(X/X_n) - f(Y/Y_n)),\quad
b^* = 200(f(Y/Y_n) - f(Z/Z_n))
\]

Обратное преобразование через \(f^{-1}\) и умножение на \(X_n, Y_n, Z_n\).

## 6. Описание алгоритма

1. Пользователь меняет источник (RGB / HSV / LAB) через slider, Entry или color picker.
2. Обновляется внутреннее состояние `_r,_g,_b` или `_h,_s,_v` или `_L,_a,_b_lab`.
3. Вызывается `update_from_*`: пересчёт двух других представлений.
4. `update_ui` / `_refresh_ui_from_state` синхронизирует Entry и Scale под флагом `_updating = True`, чтобы не было рекурсии.

## 7. Описание интерфейса

- Заголовок **COLOR MODEL CONVERTER**.
- **CURRENT COLOR** — Canvas + HEX.
- Секции **RGB**, **HSV**, **CIE LAB**: для каждой компоненты Entry + Scale.
- Кнопка **Выбрать цвет**.
- Строка **Статус**.

## 8. Реализация

| Файл | Назначение |
|------|------------|
| `color_converter.py` | `rgb_to_hsv`, `hsv_to_rgb`, `rgb_to_xyz`, `xyz_to_rgb`, `xyz_to_lab`, `lab_to_xyz`, `rgb_to_lab`, `lab_to_rgb` |
| `main.py` | Класс `ColorApp`, Tkinter GUI, валидация, anti-loop |

## 9. Обработка ошибок и clipping

- Нечисловой или пустой ввод → сообщение в статусной строке, восстановление полей из состояния.
- Значения вне диапазона → отказ при commit Entry с текстом ошибки.
- LAB → RGB вне sRGB gamut → clip + статус при commit (и при тестах `clipped=True`).

## 10. Тестирование

Файл `test_color_conversion.py`: unittest, round-trip HSV/LAB, белый/чёрный, out-of-gamut.

Запуск: `python3 -m unittest test_color_conversion.py -v`

## 11. Результаты

Приложение запускается командой `python3 main.py`, все три способа задания цвета работают, пересчёт автоматический, тесты проходят.

## 12. Вывод

Реализован конвертер для варианта №4 с корректными формулами sRGB–XYZ–Lab и классическим RGB–HSV; интерфейс позволяет наглядно сравнивать представления одного цвета и демонстрировать ограничение RGB-gamut при работе с Lab.
